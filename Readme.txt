Author: Steven Karr
Date: 12-15-15


The compiler takes as input a text file with YES code and outputs a text file with executable MIPS assembly code. The files are designated by two immediate command line arguments. Alternatively, one may use the included makefile to run the program. Struct functionality was not included. 